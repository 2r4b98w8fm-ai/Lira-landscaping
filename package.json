{
  "name": "last-seen",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run validate && tsc --noEmit && vite build",
    "build:single": "npm run validate && tsc --noEmit && vite build --config vite.singlefile.config.ts",
    "preview": "vite preview",
    "validate": "tsx scripts/validate.ts"
  },
  "devDependencies": {
    "@fontsource-variable/inter": "^5.2.5",
    "@tailwindcss/vite": "^4.1.0",
    "@types/node": "^26.1.1",
    "tailwindcss": "^4.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
